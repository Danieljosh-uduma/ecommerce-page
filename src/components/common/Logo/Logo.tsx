/**
 * Renders the main logo for the application.
 *
 * @returns {JSX.Element} The logo component containing the brand name "Bandage" wrapped in a link.
 */
export default function Logo() {
    return (
        <a href="">
            <h3 className="navbar__logo">Bandage</h3>
        </a>
    );
}
